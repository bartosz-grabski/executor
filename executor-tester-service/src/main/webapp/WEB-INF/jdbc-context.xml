<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd">

<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="locations">
        <value>classpath:tester.properties</value>
    </property>
</bean>

<bean id="dataSource" destroy-method="close"
      class="org.apache.commons.dbcp.BasicDataSource" scope="prototype">
    <property name="driverClassName" value="${agh.bit.ideafactory.jdbc.driver}"/>
    <property name="url" value="${agh.bit.ideafactory.jdbc.url}"/>
    <property name="username" value="${agh.bit.ideafactory.jdbc.username}"/>
    <property name="password" value="${agh.bit.ideafactory.jdbc.password}"/>
</bean>
</beans>